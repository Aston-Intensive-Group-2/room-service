server:
  port: 8094

spring:
  mail:
    host: smtp.mail.ru
    port: 587
    username: roomservicenotification@mail.ru
    password: NrGFWpQCWpWhNERscLDB
#    protocol: smtps
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          required:
            enable: true
      debug: true
    transport:
      protocol: smtp
#          ssl:
#            enable: true
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://${SPRING_DATASOURCE_HOST:localhost}:${SPRING_DATASOURCE_PORT:5444}/notification_db
    username: ${SPRING_DATASOURCE_USERNAME:postgres}
    password: ${SPRING_DATASOURCE_PASSWORD:password}
  application:
    name: notification-service

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate.jdbc.time_zone: UTC
      hibernate.format_sql: true
    show-sql: true

  sql:
    init:
      mode: always

eureka:
  client:
    service-url:
      defaultZone: http://${SPRING_DISCOVERY_SERVER_HOST:localhost}:8761/eureka/
  instance:
    preferIpAddress: true

logging:
  level:
    org.springframework.jdbc.datasource.DriverManagerDataSource: DEBUG
    org.springframework.orm.jpa: INFO
    org.springframework.transaction: INFO
    org.springframework.web: INFO

telegram:
  bot-token: "7944346943:AAHXs9xQ3bVHcYBDQ8me4ooDaxxrDgJLry8"
  chat-id: "310075966"

notification:
  email:
    from: roomservicenotification@mail.ru